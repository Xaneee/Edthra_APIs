# core/task_planner.py

def plan_task(title: str, goal: str):
    # Mocked logic for now
    return {
        "title": title,
        "goal": goal,
        "steps": [
            "Analyze problem",
            "Break into modules",
            "Search knowledge",
            "Generate code",
            "Test & validate"
        ],
        "status": "planning_started"
    }
